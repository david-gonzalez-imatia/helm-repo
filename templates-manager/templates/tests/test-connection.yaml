apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "templates-manager.fullname" . }}-test-connection"
  labels:
{{ include "templates-manager.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "templates-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
