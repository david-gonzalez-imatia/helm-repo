kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "templates-manager.fullname" . }}
spec:
  storageClassName: "nfs"
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Mi